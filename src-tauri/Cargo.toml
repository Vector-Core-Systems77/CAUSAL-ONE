[package]
name = "causal-one"
version = "0.1.0"
edition = "2021"

[lib]
name = "causal_one_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.0", features = [] }
